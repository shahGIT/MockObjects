package com.develogical.shopping;

/**public interface OrderTracker {
    void orderConfirmed(Order order);
    void outOfStock(Order order);
}
   */
public interface OrderTraker{
    void Orderconfirmed(Order order);
    void outOfStock(Order order);
}